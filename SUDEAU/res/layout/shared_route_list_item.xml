<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="6dip" >
    
    <ImageView
        android:id="@+id/sh_route_pic"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="TODO"        
        android:src="@drawable/ic_share_icon" />
     
     <TextView
         android:id="@+id/sh_route_name"
         android:layout_width="100dip"
         android:layout_height="wrap_content"
         android:layout_alignBottom="@+id/sh_route_pic"
         android:layout_alignTop="@+id/sh_route_pic"
         android:layout_alignParentTop="true"
         android:layout_alignWithParentIfMissing="true"         
         android:layout_toRightOf="@id/sh_route_pic"
         android:gravity="center_vertical"
         android:text="Nom de la ruta"
         android:textSize="12sp" />
     
     <ImageView
         android:id="@+id/sh_route_upload"
         android:layout_width="wrap_content"
         android:layout_height="fill_parent"
         android:layout_alignBottom="@+id/sh_route_pic"
         android:layout_alignParentTop="true"
         android:layout_alignTop="@+id/sh_route_pic"
         android:layout_marginLeft="6dip"
         android:layout_toRightOf="@id/sh_route_name"
         android:contentDescription="TODO"
         android:src="@drawable/ic_download" />

     <RatingBar
         android:id="@+id/ratBarUserRoute"
         android:layout_width="50dip"
         android:layout_height="wrap_content"
         android:layout_toRightOf="@id/sh_route_upload"
         android:layout_alignParentRight="true"
         android:layout_centerVertical="true" />

</RelativeLayout>
